C NCLFORTSTART
      REAL function cubic_interp (p, w)
      real p,w
      dimension p(4) 
C NCLEND

C NCLFORTSTART
   REAL FUNCTION bicubic_interp(p, w, x)
    REAL w, x, p, p_temp
    DIMENSION p(4,4), p_temp(4)
C NCLEND

C NCLFORTSTART
       REAL FUNCTION linear_interp(x3, x2, xi)
       REAL x1, x2, xi
C NCLEND

C NCLFORTSTART
   REAL FUNCTION neville_interp(psub, prof, inp,n)
    REAL inp, psub, prof, f
    INTEGER n
    DIMENSION psub(n), prof(n), f(n)
C NCLEND

